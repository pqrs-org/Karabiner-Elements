import SwiftUI

struct MainView: View {
    var body: some View {
        Text("MainView")
            .padding()
    }
}

struct MainView_Previews: PreviewProvider {
    static var previews: some View {
        MainView()
    }
}
