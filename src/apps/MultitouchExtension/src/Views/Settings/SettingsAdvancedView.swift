import SwiftUI

struct SettingsAdvancedView: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 25.0) {
      HStack {
        Text("SettingsAdvancedView")

        Spacer()
      }

      Spacer()
    }.padding()
  }
}

struct SettingsAdvancedView_Previews: PreviewProvider {
  static var previews: some View {
    SettingsAdvancedView()
  }
}
