#pragma once

#include "event_queue.hpp"

namespace krbn {
class simple_modifications_manipulator final {
public:
  static void manipulate(event_queue& input_event_queue,
                         event_queue& output_event_queue) {
  }
};
} // namespace krbn
