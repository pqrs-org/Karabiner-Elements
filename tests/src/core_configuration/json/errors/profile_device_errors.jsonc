[
    {
        "class": "devices",
        "input": null,
        "error": "json must be object, but is `null`"
    },

    // identifiers

    {
        "class": "devices",
        "input": {
            "identifiers": null
        },
        "error": "`identifiers` error: json must be object, but is `null`"
    },

    // ignore

    {
        "class": "devices",
        "input": {
            "ignore": null
        },
        "error": "`ignore` must be boolean, but is `null`"
    },

    // manipulate_caps_lock_led

    {
        "class": "devices",
        "input": {
            "manipulate_caps_lock_led": null
        },
        "error": "`manipulate_caps_lock_led` must be boolean, but is `null`"
    },

    // treat_as_built_in_keyboard

    {
        "class": "devices",
        "input": {
            "treat_as_built_in_keyboard": null
        },
        "error": "`treat_as_built_in_keyboard` must be boolean, but is `null`"
    },

    // disable_built_in_keyboard_if_exists

    {
        "class": "devices",
        "input": {
            "disable_built_in_keyboard_if_exists": null
        },
        "error": "`disable_built_in_keyboard_if_exists` must be boolean, but is `null`"
    },

    // mouse_flip_x

    {
        "class": "devices",
        "input": {
            "mouse_flip_x": null
        },
        "error": "`mouse_flip_x` must be boolean, but is `null`"
    },

    // mouse_flip_y

    {
        "class": "devices",
        "input": {
            "mouse_flip_y": null
        },
        "error": "`mouse_flip_y` must be boolean, but is `null`"
    },

    // mouse_flip_vertical_wheel

    {
        "class": "devices",
        "input": {
            "mouse_flip_vertical_wheel": null
        },
        "error": "`mouse_flip_vertical_wheel` must be boolean, but is `null`"
    },

    // mouse_flip_horizontal_wheel

    {
        "class": "devices",
        "input": {
            "mouse_flip_horizontal_wheel": null
        },
        "error": "`mouse_flip_horizontal_wheel` must be boolean, but is `null`"
    },

    // mouse_swap_xy

    {
        "class": "devices",
        "input": {
            "mouse_swap_xy": null
        },
        "error": "`mouse_swap_xy` must be boolean, but is `null`"
    },

    // mouse_swap_wheels

    {
        "class": "devices",
        "input": {
            "mouse_swap_wheels": null
        },
        "error": "`mouse_swap_wheels` must be boolean, but is `null`"
    },

    // game_pad_swap_sticks

    {
        "class": "devices",
        "input": {
            "game_pad_swap_sticks": null
        },
        "error": "`game_pad_swap_sticks` must be boolean, but is `null`"
    },

    // game_pad_xy_stick_deadzone

    {
        "class": "devices",
        "input": {
            "game_pad_xy_stick_deadzone": null
        },
        "error": "`game_pad_xy_stick_deadzone` must be number, but is `null`"
    },

    // game_pad_wheels_stick_deadzone

    {
        "class": "devices",
        "input": {
            "game_pad_wheels_stick_deadzone": null
        },
        "error": "`game_pad_wheels_stick_deadzone` must be number, but is `null`"
    },

    // game_pad_xy_stick_interval_milliseconds_formula

    {
        "class": "devices",
        "input": {
            "game_pad_xy_stick_interval_milliseconds_formula": null
        },
        "error": "`game_pad_xy_stick_interval_milliseconds_formula` must be array of string, or string, but is `null`"
    },

    // game_pad_stick_wheels_interval_milliseconds

    {
        "class": "devices",
        "input": {
            "game_pad_stick_wheels_interval_milliseconds": null
        },
        "error": "`game_pad_stick_wheels_interval_milliseconds` must be number, but is `null`"
    },

    // game_pad_stick_x_formula

    {
        "class": "devices",
        "input": {
            "game_pad_stick_x_formula": null
        },
        "error": "`game_pad_stick_x_formula` must be array of string, or string, but is `null`"
    },

    // game_pad_stick_y_formula

    {
        "class": "devices",
        "input": {
            "game_pad_stick_y_formula": null
        },
        "error": "`game_pad_stick_y_formula` must be array of string, or string, but is `null`"
    },

    // game_pad_stick_vertical_wheel_formula

    {
        "class": "devices",
        "input": {
            "game_pad_stick_vertical_wheel_formula": null
        },
        "error": "`game_pad_stick_vertical_wheel_formula` must be array of string, or string, but is `null`"
    },

    // game_pad_stick_horizontal_wheel_formula

    {
        "class": "devices",
        "input": {
            "game_pad_stick_horizontal_wheel_formula": null
        },
        "error": "`game_pad_stick_horizontal_wheel_formula` must be array of string, or string, but is `null`"
    },

    // simple_modifications

    {
        "class": "devices",
        "input": {
            "simple_modifications": null
        },
        "error": "`simple_modifications` error: json must be array or object, but is `null`"
    },

    // fn_function_keys

    {
        "class": "devices",
        "input": {
            "fn_function_keys": null
        },
        "error": "`fn_function_keys` error: json must be array or object, but is `null`"
    }
]
